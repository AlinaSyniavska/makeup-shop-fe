import React, { FC } from "react";

import { AuthForm } from "../../components";

const LoginPage: FC = () => {
  return (
    <React.Fragment>
      <AuthForm />
    </React.Fragment>
  );
};

export { LoginPage };
