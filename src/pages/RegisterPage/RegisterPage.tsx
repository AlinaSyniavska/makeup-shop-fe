import React, { FC } from "react";

import { RegisterForm } from "../../components";

const RegisterPage: FC = () => {
  return (
    <React.Fragment>
      <RegisterForm />
    </React.Fragment>
  );
};

export { RegisterPage };
